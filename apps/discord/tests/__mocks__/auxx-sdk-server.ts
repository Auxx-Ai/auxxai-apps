// Stub for @auxx/sdk/server — the real module is injected by the platform at runtime.
// Tests use vi.mock('@auxx/sdk/server') to provide per-test implementations.
export function getOrganizationConnection() {
  return null
}
